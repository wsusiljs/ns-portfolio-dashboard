def style_fig(fig, height=450):

    fig.update_layout(
        height=height,
        margin=dict(
            l=20,
            r=20,
            t=40,
            b=20
        )
    )

    return fig
